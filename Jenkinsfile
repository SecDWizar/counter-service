pipeline {
    agent any 
    stages {
        stage('build local container') {
            steps {
                echo 'Hello world 6!'
                sh 'docker --version'
            }
        }
    }
}
